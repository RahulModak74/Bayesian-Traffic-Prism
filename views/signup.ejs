<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Cybersecurity Platform</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f8ff;
            padding: 50px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            flex-direction: column;
        }
        .container {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            text-align: center;
        }
        .icon-container {
            text-align: center;
            margin-bottom: 20px;
        }
        .icon-container i {
            font-size: 60px;
            margin: 10px;
            color: #007bff;
        }
        .cdp-explanation p {
            margin-bottom: 20px;
            font-size: 1.1em;
            color: #333;
        }
        form {
            width: 100%;
            max-width: 300px;
            margin: 0 auto;
        }
        input[type="text"], input[type="password"] {
            margin-bottom: 15px;
            padding: 12px;
            border-radius: 5px;
            border: 1px solid #ddd;
            width: 100%;
            box-sizing: border-box;
        }
        input[type="submit"], button {
            padding: 12px;
            background-color: #0056b3;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            box-sizing: border-box;
        }
        input[type="submit"]:hover, button:hover {
            background-color: #003d80;
        }
        h1 {
            margin-bottom: 20px;
            font-size: 2.5em;
            color: #0056b3;
        }
        h2 {
            margin-bottom: 20px;
            color: #007bff;
        }
        .signup-btn {
            margin-top: 15px;
            padding: 12px;
            background-color: #28a745;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            box-sizing: border-box;
        }
        .signup-btn:hover {
            background-color: #218838;
        }
        .error-message {
            color: red;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Hero Section with Icons -->
        <h1>Sign Up</h1>
        <div class="icon-container">
            <i class="fas fa-user-plus"></i>
        </div>

        <!-- Error Message -->
        <% if (errorMessage) { %>
            <p class="error-message"><%= errorMessage %></p>
        <% } %>

        <!-- Sign Up Form -->
        <form action="/signup" method="POST">
            <input type="text" name="username" placeholder="Username" required /><br>
            <input type="password" name="password" placeholder="Password" required /><br>
            <input type="text" name="hostname" placeholder="Hostname" required /><br>
            <input type="text" name="version" placeholder="Version (optional)" /><br>
            <button type="submit">Sign Up</button>
        </form>

        <!-- Go to Login Page Button -->
        <form action="/" method="GET" style="margin-top: 15px;">
            <button type="submit">Go to Login Page</button>
        </form>
    </div>
</body>
</html>
